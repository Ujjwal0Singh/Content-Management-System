# This file is informational only. Each service reads its own .env file
# (backend/.env, admin-frontend/.env, public-frontend/.env).
# Copy the .env.example inside each folder and fill in real values.
